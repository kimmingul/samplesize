---
description: Generate a power curve (power vs N, or vs effect size) for the most recent calculation.
---

Invoke the `samplesize-report` skill to generate a power-curve PNG
for the most recent calculation. The skill knows the underlying CLI
syntax (`samplesize report <audit_json> --kind power-curve`).
Arguments: `$ARGUMENTS`.
