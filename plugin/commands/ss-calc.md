---
description: Compute sample size or power for a named method.
---

Invoke the `samplesize-calculate` skill. User arguments (method id and
any inline parameters): `$ARGUMENTS`.

If no method id is given, first hand off to `samplesize-design`.
